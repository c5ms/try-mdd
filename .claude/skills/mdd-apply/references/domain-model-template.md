# {领域名称}领域模型

> 最后更新：{日期}

---

## 1 领域词典

| 概念 | 是 | 不是 |
|------|------|------|
| {概念} | {严格定义} | {反例} |

---

## 2 状态流转

| 当前状态 | 触发条件 | 目标状态 | 副作用 | 禁止动作 | 禁止原因 |
|----------|----------|----------|----------|----------|----------|
| `{状态A}` | {触发条件} | `{状态B}` | {副作用描述} | {禁止操作} | {禁止原因} |
| `{状态B}`（终态） | - | - | - | 所有操作 | 终态不可变更 |

---

### 状态图

```mermaid
stateDiagram-v2
  [*] --> {状态A}: 创建
  {状态A} --> {状态B}: {触发条件}
  {状态B} --> [*]
```

---

## 3 实体定义

### 实体关系图

```mermaid
erDiagram
  {实体名} {
    string id PK
    string code
    string name
    enum status
  }
  
  {实体A} ||--o{ {实体B} : contains
```

### {实体名}（聚合根）

| 属性 | 类型 | 必填 | 说明 |
|------|------|------|------|
| id | string | ✓ | 唯一标识 |
| code | string | ✓ | 编码 |
| name | string | ✓ | 名称 |
| status | enum | ✓ | 状态 |

---

## 4 业务规则

| 规则ID | 规则名 | WHEN | THEN | 约束 |
|--------|--------|------|------|------|
| R001 | {规则名} | {触发条件} | {执行动作} | {约束条件} |

---

### 不变式

| 不变式ID | 不变式名 | 约束条件 | 防止场景 |
|----------|----------|----------|----------|
| I001 | {不变式名} | {约束条件} | {冲突场景} |

---

## 5 领域事件

| 事件名 | 携带数据 | 预期消费者 | 执行动作 |
|--------|----------|----------|----------|
| {事件名} | `{字段1}, {字段2}` | {消费者} | {执行动作} |

---

## 6 聚合边界

| 聚合名 | 聚合根 | 内部实体 |
|--------|--------|----------|
| {聚合名} | {实体名} | {实体列表} |

---

## 7 上下游关系图

```mermaid
graph LR
  {上游聚合} -->|{引用方式}| {当前聚合}
  {当前聚合} -->|{引用方式}| {下游聚合}
  
  style {当前聚合} fill:#e1f5fe
```

---

## 8 用例

| 用例 | 角色 | 操作 | 目标 |
|------|------|------|------|
| {用例} | {角色} | {操作} | {目标} |

---

## 9 补充流程图

（暂无）

---

## 10 待定任务

| # | 待定内容 | 来源 | 状态 |
|---|----------|------|------|
| 1 | {待定项} | {来源} | 待确认 |

---

## 11 附录：用户自定义内容

> 来源：draft/{domain}.md

（如有用户自定义内容，在此章节追加）

### {用户自定义章节1}

{用户自定义内容}

### {用户自定义章节2}

{用户自定义内容}